'use strict';

var React = require('react');
var ReactDOM = require('react-dom');

var ButtonLogin = require('./SearchRepo');

ReactDOM.render(<ButtonLogin />, document.getElementById('search-container'));